name: CI
on:
 push:
   branches: master
 pull_request:
   branches: master

jobs:
 conviso-sast:
   runs-on: ubuntu-latest
   container:
     image: convisoappsec/convisocli
     env:
       FLOW_API_KEY:  S-Av3MIHR0NKRpJoMMMQ8OCZzFfrPc5BwoJbMKeohnk
   steps:
   - uses: actions/checkout@v3

   - name: Run AST
     run: conviso ast run
